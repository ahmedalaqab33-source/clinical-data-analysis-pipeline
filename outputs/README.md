# Generated outputs

The pipeline writes a descriptive summary and a JSON quality-control report to this directory by default. Generated files are ignored by Git because they may reveal sensitive labels or aggregates.

Review disclosure risk and institutional policy before sharing any output.
